import "package:flutter/material.dart";

class EducationModule extends StatelessWidget {
  const EducationModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
