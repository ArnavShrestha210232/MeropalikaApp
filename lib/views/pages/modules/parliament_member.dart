import "package:flutter/material.dart";

class ParliamentModule extends StatelessWidget {
  const ParliamentModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
