import "package:flutter/material.dart";

class InformationModule extends StatelessWidget {
  const InformationModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
