import "package:flutter/material.dart";

class PoliceStationModule extends StatelessWidget {
  const PoliceStationModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
