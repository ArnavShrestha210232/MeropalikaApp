import "package:flutter/material.dart";

class ProtectedAreaModule extends StatelessWidget {
  const ProtectedAreaModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
