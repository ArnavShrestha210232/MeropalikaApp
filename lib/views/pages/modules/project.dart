import "package:flutter/material.dart";

class ProjectModule extends StatelessWidget {
  const ProjectModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
