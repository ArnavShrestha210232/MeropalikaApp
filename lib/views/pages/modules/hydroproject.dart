import "package:flutter/material.dart";

class HydroProjectModule extends StatelessWidget {
  const HydroProjectModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
