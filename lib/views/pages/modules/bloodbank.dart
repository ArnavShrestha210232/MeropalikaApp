import "package:flutter/material.dart";

class BloodBankModule extends StatelessWidget {
  const BloodBankModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
