import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class AddBloodBank extends StatelessWidget {
  const AddBloodBank({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'रक्त बैंक ', child: Container());
  }
}
