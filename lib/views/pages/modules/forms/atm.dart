import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class AddAtmDetails extends StatelessWidget {
  const AddAtmDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'एटिएम', child: Container());
  }
}
