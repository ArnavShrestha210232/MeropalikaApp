import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class AddHistoricDetails extends StatelessWidget {
  const AddHistoricDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'एटिएम', child: Container());
  }
}
