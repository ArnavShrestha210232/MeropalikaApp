import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class AddIndustriesDetails extends StatelessWidget {
  const AddIndustriesDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'एटिएम', child: Container());
  }
}
