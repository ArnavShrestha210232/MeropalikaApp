import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class AddParliamentDetails extends StatelessWidget {
  const AddParliamentDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'एटिएम', child: Container());
  }
}
