import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class SystemDepartmentName extends StatelessWidget {
  const SystemDepartmentName({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'हवाईअडा', child: Container());
  }
}
