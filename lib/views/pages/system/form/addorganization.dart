import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class SystemOrganization extends StatelessWidget {
  const SystemOrganization({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'हवाईअडा', child: Container());
  }
}
