import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class SystemProvince extends StatelessWidget {
  const SystemProvince({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'हवाईअडा', child: Container());
  }
}
