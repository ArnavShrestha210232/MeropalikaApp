import 'package:flutter/material.dart';

import '../../../widgets/basicpage.dart';

class SystemFiscalYear extends StatelessWidget {
  const SystemFiscalYear({super.key});

  @override
  Widget build(BuildContext context) {
    return BasicPage(title: 'हवाईअडा', child: Container());
  }
}
